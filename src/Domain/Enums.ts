export enum Gender {
    Male,
    Female,
}

export enum UserAccountStatus {
    ACTIVE = 0,
    DEACTIVATED = 1,
}

export enum UserType {
    SUPERADMIN = 0,
    ADMIN = 1,
    USER = 2,
}

export enum InviteStatus {
    PENDING = 0,
    ACCEPTED = 1,
}


export enum OTPStatus {
    UNUSED = 0,
    USED = 1,
}